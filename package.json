{
  "name": "template-monorepo",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "concurrently -n admin,api -c green,blue --pad-prefix \"pnpm --filter admin dev\" \"pnpm --filter api dev\"",
    "dev:server": "pnpm --filter api dev",
    "dev:web": "pnpm --filter admin dev",
    "start:server": "pnpm --filter api start",
    "build:server": "pnpm --filter api build",
    "build:web": "pnpm --filter admin build",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter api test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@prisma/engines",
      "@scarf/scarf",
      "bcrypt",
      "esbuild",
      "msgpackr-extract",
      "prisma",
      "unrs-resolver"
    ]
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
